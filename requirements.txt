# Data
pandas>=2.0
numpy>=1.26

# Database
sqlalchemy>=2.0

# Machine Learning — pinned exactly: models/*.pkl were trained/saved with
# these versions, and a mismatched xgboost/lightgbm/scikit-learn can fail
# to unpickle them (or load silently wrong). Bump deliberately, then retrain
# and recommit the .pkl files together with any version bump here.
scikit-learn==1.9.0
xgboost==3.4.1
lightgbm==4.7.0
shap==0.52.0
joblib==1.5.3
optuna>=3.0

# AI / Agents
langchain>=0.2
langgraph>=0.1
langchain-openai>=0.1
openai>=1.0

# Visualization & App
streamlit>=1.35
plotly>=5.18
matplotlib>=3.8
seaborn>=0.13

# Export
reportlab>=4.0
kaleido>=0.2

# Dev & Testing
pytest>=8.0
pytest-cov>=4.0
jupyter>=1.0
ipykernel>=6.0
python-dotenv>=1.0
